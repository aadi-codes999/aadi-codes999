# Aditya's GitHub Profile

Put these files in the profile repository `aadi-codes999/aadi-codes999`.

- `README.md` — finished profile design.
- `wlc_adi_github.png` — supplied banner.
- `settings.json` — matching dark/icy-blue 3D contribution palette.
- `.github/workflows/profile-3d.yml` — generates and refreshes the 3D graph.

After pushing, open **Actions → GitHub Profile 3D Contributions → Run workflow** once.
The workflow then updates the graph automatically on its schedule.

The Featured Projects section was removed as requested.
